# Edge types

Use `cites`, `extends`, `uses_dataset`, `compares_with`, `contradicts`, `addresses_limitation`, `same_author_line`, and `same_lab_line`. Record the source sentence or metadata endpoint for each edge. Mark inferred intellectual relationships as inference rather than citation fact.
